/* var letters = "abcd"

for (var i = 0; i < letters.length; i++) {
    console.log(letters[i]);
}\
*/

var greet = "hello"
var user = " James"
var user1 = " Mike"

//console.log(greet.pop());

console.log(greet + user);
console.log(greet.concat(user1)); // the concat method accepts whatever value you want to join or concat