/* var train1 = ["wheat", "barley", "potato", "salt", "rocks"]
console.log(train1[2]);
*/

// arrays are iterable

var veggies = ["onions", "carrot", "cucumber"];

// to output the length of array

console.log(veggies.length);

// to output individual index

console.log(veggies[1]);

// adding a for loop and making the array iterable

for (var i = 0; i < veggies.length; i++) {
    console.log(veggies[2]);
}